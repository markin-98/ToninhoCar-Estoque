{
  "buildCommand": "cd src/app && npm install && npx expo export --platform web && node scripts/pwa-inject.mjs",
  "outputDirectory": "src/app/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
